import React from 'react';
import ReactDOM from 'react-dom';
import AppClient from './apps/AppClient';

const rootElement = document.getElementById('root');

ReactDOM.render(<AppClient />, rootElement);
